# Admin icons

The admin sidebar prefers `leveling.png` and `ticket.png` from this folder when those assets are present. If they are not available, the admin UI falls back to embedded PNG icon data so the Leveling and Tickets tabs still display image icons instead of text badges.
