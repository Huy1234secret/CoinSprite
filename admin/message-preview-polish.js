(() => {
  // Kept as a compatibility entry point. Direct preview editing is handled by
  // message-inline-editor.js so only one script owns message interactions.
  window.__coinSpritePreviewPolishDisabled = true;
})();
