const { renderPublishedLevelCard, saveLevelCardDesign, renderLevelCard } = require('./src/leveling');

async function test() {
  console.log("Mocking scenario where fetch external succeeds but returns an image that lacks something?");
}
test();
