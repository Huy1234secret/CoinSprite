const fs = require('fs');
const path = require('path');
const zlib = require('zlib');
const { AttachmentBuilder, MessageFlags, SlashCommandBuilder } = require('discord.js');

const COMPONENTS_V2_FLAG = MessageFlags.IsComponentsV2 ?? 32768;
const EPHEMERAL_FLAG = MessageFlags.Ephemeral ?? 64;
const WHITE_ACCENT = 0xffffff;
const BLACK_ACCENT = 0x111214;
const GREEN_ACCENT = 0x57f287;
const RED_ACCENT = 0xed4245;
const BUTTON_STYLE_SECONDARY = 2;
const FISHING_CHANNEL_ID = '1506684299934437517';
const LOCATION = 'Calm Fishing Lake';
const STORE_PATH = path.join(__dirname, '..', 'data', 'fishing-game.json');
const FISH_IMAGE_DIR = path.join(__dirname, 'Fish Png');
const LOCATION_IMAGE_DIR = path.join(__dirname, 'Location Png');
const CALM_LAKE_XLSX = path.join(__dirname, 'Calm Fishing Lake.xlsx');
const FISH_COIN = '<:CRFishCoin:1506701069990891751>';
const SPIN_FISH = '<a:SBSpinFish:1506656617469317251>';
const FISH_EMOJI = '<:SBFish:1506659437165936690>';
const GOLDEN_FISH_EMOJI = '<:SBGoldenFish:1506659439502168245>';
const RAINBOW_FISH_EMOJI = '<:SBRainbowFish:1506660311380398211>';
const BAR = {
  start: '<:SBFPB1:1506679960616697948>',
  middle: '<:SBFPB2:1506679958658089072>',
  middleEmpty: '<:SBFPB2E:1506679954405064774>',
  end: '<:SBFPB3:1506681043208376492>',
  endEmpty: '<:SBFPB3E:1506679951682965658>',
};

const FISH = [
  ['<:F1Bluegill:1506653228245455039>', 'F1 Bluegill', 'common', 0.1, 1.0, 8, 10, 1],
  ['<:F1CommonCarp:1506653230376030318>', 'F1 Common Carp', 'common', 1.0, 8.0, 12, 18, 1],
  ['<:F1FatheadMinnow:1506653232146022531>', 'F1 Fathead Minnow', 'common', 0.02, 0.15, 5, 5, 1],
  ['<:F1YellowPerch:1506653234419466290>', 'F1 Yellow Perch', 'common', 0.1, 1.5, 10, 12, 1],
  ['<:F2BlackCrappie:1506653236512166019>', 'F2 Black Crappie', 'uncommon', 0.2, 2.0, 22, 25, 2],
  ['<:F2ChannelCatfish:1506653238605254798>', 'F2 Channel Catfish', 'uncommon', 1.5, 12.0, 30, 35, 2],
  ['<:F2RainbowTrout:1506653240756801708>', 'F2 Rainbow Trout', 'uncommon', 0.5, 4.0, 35, 40, 2],
  ['<:F3LargemouthBass:1506653242506088478>', 'F3 Largemouth Bass', 'rare', 0.8, 6.0, 65, 65, 3],
  ['<:F3Walleye:1506653246255792198>', 'F3 Walleye', 'rare', 0.7, 5.5, 75, 75, 3],
  ['<:F4NorthernPike:1506653248147292290>', 'F4 Northern Pike', 'epic', 2.0, 15.0, 140, 120, 5],
  ['<:F5LakeSturgeon:1506653250621935827>', 'F5 Lake Sturgeon', 'legendary', 8.0, 60.0, 350, 220, 8],
  ['<:F6GoldenMahseer:1506653252530212975>', 'F6 Golden Mahseer', 'mythical', 3.0, 25.0, 850, 400, 12],
  ['<:F7AsianArowana:1506653254677954700>', 'F7 Asian Arowana', 'secret', 2.0, 10.0, 2500, 750, 20],
].map(([emoji, name, rarity, minWeight, maxWeight, sellValue, powerReq, durDamage]) => ({ id: normalizeId(name), emoji, name, rarity, minWeight, maxWeight, sellValue, powerReq, durDamage }));
const FISH_BY_ID = new Map(FISH.map((fish) => [fish.id, fish]));
const FISH_BY_NAME = new Map(FISH.map((fish) => [normalizeName(fish.name), fish]));
const RARITY_BUTTONS = { common: 2, uncommon: 3, rare: 4, epic: 6, legendary: 8, mythical: 10, secret: 15 };
const RARITY_WEIGHTS = { common: 65, uncommon: 22, rare: 9, epic: 3, legendary: 0.8, mythical: 0.18, secret: 0.02 };
const REEL_EMOJIS = ['🐟', '🎣', '🪝', '🫧', '🌊', '🐠', '🐡', '🦈', '🦑', '🦀', '🐚', '🪙', '⭐', '💧', '🍀'];
const ITEMS = {
  wooden_fishing_rod: { id: 'wooden_fishing_rod', name: 'Wooden Fishing Rod', emoji: '🎣', type: 'Gear/Tool', rarity: 'common', value: 0, powerMin: 3, powerMax: 5, durability: null },
  bamboo_fishing_rod: { id: 'bamboo_fishing_rod', name: 'Bamboo Fishing Rod', emoji: '<:IGBambooFishingRod:1507183020485120120>', type: 'Gear/Tool', rarity: 'common', value: 350, powerMin: 8, powerMax: 12, durability: 40 },
  steel_fishing_rod: { id: 'steel_fishing_rod', name: 'Steel Fishing Rod', emoji: '<:IGSteelFishingRod:1507183025643847802>', type: 'Gear/Tool', rarity: 'common', value: 1250, powerMin: 18, powerMax: 25, durability: 85 },
  carbon_fishing_rod: { id: 'carbon_fishing_rod', name: 'Carbon Fishing Rod', emoji: '<:IGCarbonFishingRod:1507183023139979395>', type: 'Gear/Tool', rarity: 'common', value: 5000, powerMin: 30, powerMax: 40, durability: 115 },
};
const VARIANTS = [
  { key: 'Normal', emoji: FISH_EMOJI, chance: 89, multiplier: 1 },
  { key: 'Golden', emoji: GOLDEN_FISH_EMOJI, chance: 10, multiplier: 2 },
  { key: 'Rainbow', emoji: RAINBOW_FISH_EMOJI, chance: 1, multiplier: 5 },
];
const SEASONS = [
  { key: 'Spring', emoji: '🌸' },
  { key: 'Summer', emoji: '☀️' },
  { key: 'Fall', emoji: '🍂' },
  { key: 'Winter', emoji: '❄️' },
];
const TIMES = { Morning: '🌅', Noon: '☀️', Afternoon: '🌇', Night: '🌙' };
const WEATHER_CHANCES = {
  Spring: { Morning: [['Sunny', '☀', 40], ['Rain', '☔', 25], ['Storm', '🌀', 10], ['Thunderstorm', '⚡', 5], ['Fog', '≋', 10], ['Windy', '↝', 10]], Noon: [['Sunny', '☀', 50], ['Rain', '☔', 25], ['Storm', '🌀', 10], ['Thunderstorm', '⚡', 5], ['Windy', '↝', 10]], Afternoon: [['Sunny', '☀', 45], ['Rain', '☔', 25], ['Storm', '🌀', 12], ['Thunderstorm', '⚡', 6], ['Windy', '↝', 12]], Night: [['Night Clear Sky', '★', 40], ['Full Moon Night', '○', 8], ['Bloodmoon', '●', 1], ['Rain', '☔', 25], ['Storm', '🌀', 10], ['Thunderstorm', '⚡', 5], ['Fog', '≋', 6], ['Windy', '↝', 5]] },
  Summer: { Morning: [['Sunny', '☀', 55], ['Rain', '☔', 15], ['Storm', '🌀', 8], ['Thunderstorm', '⚡', 5], ['Fog', '≋', 7], ['Windy', '↝', 10]], Noon: [['Sunny', '☀', 45], ['Rain', '☔', 10], ['Storm', '🌀', 5], ['Thunderstorm', '⚡', 5], ['Windy', '↝', 10], ['Heatwave', '🔥', 25]], Afternoon: [['Sunny', '☀', 40], ['Rain', '☔', 12], ['Storm', '🌀', 6], ['Thunderstorm', '⚡', 6], ['Windy', '↝', 10], ['Heatwave', '🔥', 26]], Night: [['Night Clear Sky', '★', 50], ['Full Moon Night', '○', 8], ['Bloodmoon', '●', 1], ['Rain', '☔', 15], ['Storm', '🌀', 8], ['Thunderstorm', '⚡', 6], ['Fog', '≋', 5], ['Windy', '↝', 7]] },
  Fall: { Morning: [['Sunny', '☀', 35], ['Rain', '☔', 30], ['Storm', '🌀', 12], ['Fog', '≋', 13], ['Windy', '↝', 10]], Noon: [['Sunny', '☀', 45], ['Rain', '☔', 30], ['Storm', '🌀', 10], ['Windy', '↝', 15]], Afternoon: [['Sunny', '☀', 40], ['Rain', '☔', 30], ['Storm', '🌀', 12], ['Windy', '↝', 18]], Night: [['Night Clear Sky', '★', 35], ['Full Moon Night', '○', 10], ['Bloodmoon', '●', 2], ['Rain', '☔', 30], ['Storm', '🌀', 10], ['Fog', '≋', 8], ['Windy', '↝', 5]] },
  Winter: { Morning: [['Sunny', '☀', 30], ['Snow', '❄', 30], ['Storm', '🌀', 10], ['Fog', '≋', 20], ['Windy', '↝', 10]], Noon: [['Sunny', '☀', 40], ['Snow', '❄', 35], ['Storm', '🌀', 10], ['Windy', '↝', 15]], Afternoon: [['Sunny', '☀', 35], ['Snow', '❄', 40], ['Storm', '🌀', 10], ['Windy', '↝', 15]], Night: [['Night Clear Sky', '★', 30], ['Full Moon Night', '○', 12], ['Bloodmoon', '●', 2], ['Snow', '❄', 35], ['Storm', '🌀', 8], ['Fog', '≋', 8], ['Windy', '↝', 5]] },
};
const COLUMN_MAP = {
  Spring: { Morning: { Sunny: 'C', Rain: 'D', Storm: 'E', Thunderstorm: 'F', Fog: 'G', Windy: 'H' }, Noon: { Sunny: 'I', Rain: 'J', Storm: 'K', Thunderstorm: 'L', Windy: 'M' }, Afternoon: { Sunny: 'N', Rain: 'O', Storm: 'P', Thunderstorm: 'Q', Windy: 'R' }, Night: { 'Night Clear Sky': 'S', 'Full Moon Night': 'T', Bloodmoon: 'U', Rain: 'V', Storm: 'W', Thunderstorm: 'X', Fog: 'Y', Windy: 'Z' } },
  Summer: { Morning: { Sunny: 'AA', Rain: 'AB', Storm: 'AC', Thunderstorm: 'AD', Fog: 'AE', Windy: 'AF' }, Noon: { Sunny: 'AG', Rain: 'AH', Storm: 'AI', Thunderstorm: 'AJ', Windy: 'AK', Heatwave: 'AL' }, Afternoon: { Sunny: 'AM', Rain: 'AN', Storm: 'AO', Thunderstorm: 'AP', Windy: 'AQ', Heatwave: 'AR' }, Night: { 'Night Clear Sky': 'AS', 'Full Moon Night': 'AT', Bloodmoon: 'AU', Rain: 'AV', Storm: 'AW', Thunderstorm: 'AX', Fog: 'AY', Windy: 'AZ' } },
  Fall: { Morning: { Sunny: 'BA', Rain: 'BB', Storm: 'BC', Fog: 'BD', Windy: 'BE' }, Noon: { Sunny: 'BF', Rain: 'BG', Storm: 'BH', Windy: 'BI' }, Afternoon: { Sunny: 'BJ', Rain: 'BK', Storm: 'BL', Windy: 'BM' }, Night: { 'Night Clear Sky': 'BN', 'Full Moon Night': 'BO', Bloodmoon: 'BP', Rain: 'BQ', Storm: 'BR', Fog: 'BS', Windy: 'BT' } },
  Winter: { Morning: { Sunny: 'BU', Snow: 'BV', Storm: 'BW', Fog: 'BX', Windy: 'BY' }, Noon: { Sunny: 'BZ', Snow: 'CA', Storm: 'CB', Windy: 'CC' }, Afternoon: { Sunny: 'CD', Snow: 'CE', Storm: 'CF', Windy: 'CG' }, Night: { 'Night Clear Sky': 'CH', 'Full Moon Night': 'CI', Bloodmoon: 'CJ', Snow: 'CK', Storm: 'CL', Fog: 'CM', Windy: 'CN' } },
};
const WEATHER_DURATIONS = { Sunny: [15, 30], Rain: [10, 22], Storm: [8, 16], Thunderstorm: [5, 12], Fog: [8, 18], Windy: [10, 20], Snow: [10, 24], Heatwave: [8, 15], 'Night Clear Sky': [15, 30], 'Full Moon Night': [10, 20], Bloodmoon: [5, 10] };
const WEATHER_EFFECTS = { Sunny: {}, Rain: { lureSeconds: -2, powerMultiplier: 1.05 }, Storm: { lureSeconds: 4, powerMultiplier: 1.2, durMultiplier: 1.5 }, Thunderstorm: { lureSeconds: 10, powerMultiplier: 1.4, durMultiplier: 2, lightningBreakChance: 0.15 }, Fog: { lureSeconds: 2, powerMultiplier: 1.1 }, Windy: { lureSeconds: 1, powerMultiplier: 1.25, durMultiplier: 1.4, escapeChance: 0.1 }, Snow: { lureSeconds: 6, powerMultiplier: 1.1, durMultiplier: 1.1 }, Heatwave: { lureSeconds: 2, powerMultiplier: 1.15, durMultiplier: 1.25 }, 'Night Clear Sky': { powerMultiplier: 0.85 }, 'Full Moon Night': { goldenBonus: 0.1 }, Bloodmoon: { lureSeconds: 5, powerMultiplier: 1.6, durMultiplier: 3, escapeChance: 0.25 } };
const WEATHER_TEXT = { Sunny: ['No effects.'], Rain: ['Fish become more abundant when it rains.', "It's harder to catch a fish.", 'Higher rarity fish chance.'], Storm: ['Becareful when go fishing, your fishing rod can BREAK easily!', "Fish doesn't like this weather... become less abundant.", "It's more harder to catch a fish.", 'Higher rarity fish chance.'], Thunderstorm: ['Why? Why go fishing during this time?', "It's rare to see a fish during this time.", "It's EVEN harder to catch a fish.", 'But Even higher rarity fish chance.'], Fog: ['Fish may not see your hook clearly.', 'Harder to catch a fish', 'Higher fish chance.'], Windy: ["It's hard to be balance, becareful with your fishing rod.", 'Those mini waves will make fish hard to bite your hook.', 'Hard to catch a fish', 'Fish has a chance to escape while trying to catch', 'Higher fish rarity chance'], Snow: ['Fishing rod is a little bit easier to break now.', 'Fish doesnt like cold woter.', 'Hard to catch a fish.', 'Higher rarity fish chance.'], Heatwave: ['Fishing rod is a little bit easier to break now.', 'Fish may not like being cooked alive.', 'Harder to catch a fish.', 'Higher fish rarity chance'], 'Night Clear Sky': ['Easier to catch a fish, wonder why?', 'Some fish that only catchable during night started appearing.'], 'Full Moon Night': ['Even higher fish rarity chance.', 'We have found out Golden variant started appearing.'], Bloodmoon: ["it's beautiful but dangerous, one mistake can make your fishing rod broke immediately", 'seems like fish doesnt really like this weather', 'Hard to catch fish', 'EVEN HIGHER fish rarity chance.'] };

const activeGames = new Map();
const inventoryFilters = new Map();
const barrelFilters = new Map();
let weatherTimerStarted = false;
let xlsxCache = null;

function normalizeId(value) { return String(value || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '_').replace(/^_+|_+$/g, ''); }
function normalizeName(value) { return String(value || '').trim().toLowerCase().replace(/\s+/g, ' '); }
function randomInt(min, max) { return Math.floor(Math.random() * ((max - min) + 1)) + min; }
function randomFloat(min, max) { return min + (Math.random() * (max - min)); }
function weightedPick(entries) { const valid = entries.filter((entry) => Number(entry.weight) > 0); const total = valid.reduce((sum, entry) => sum + Number(entry.weight), 0); if (total <= 0) return valid[0]?.value ?? null; let roll = Math.random() * total; for (const entry of valid) { roll -= Number(entry.weight); if (roll <= 0) return entry.value; } return valid[valid.length - 1]?.value ?? null; }
function emptyState() { return { users: {}, weather: {}, forecasts: {} }; }
function ensureStoreFile() { const dir = path.dirname(STORE_PATH); if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true }); if (!fs.existsSync(STORE_PATH)) fs.writeFileSync(STORE_PATH, JSON.stringify(emptyState(), null, 2), 'utf8'); }
function loadState() { ensureStoreFile(); try { const state = JSON.parse(fs.readFileSync(STORE_PATH, 'utf8')); return { ...emptyState(), ...(state && typeof state === 'object' ? state : {}) }; } catch { return emptyState(); } }
function saveState(state) { ensureStoreFile(); fs.writeFileSync(STORE_PATH, JSON.stringify({ ...emptyState(), ...state }, null, 2), 'utf8'); }
function ensureUser(state, userId) { if (!state.users[userId]) state.users[userId] = { fishCoins: 0, inventory: {}, fishBarrel: [], equippedRodId: 'wooden_fishing_rod', equippedBaitId: null, location: LOCATION, fishCapacity: 10 }; const user = state.users[userId]; user.inventory = user.inventory && typeof user.inventory === 'object' ? user.inventory : {}; user.fishBarrel = Array.isArray(user.fishBarrel) ? user.fishBarrel : []; user.fishCoins = Math.max(0, Math.floor(Number(user.fishCoins) || 0)); user.fishCapacity = Math.max(10, Math.floor(Number(user.fishCapacity) || 10)); if (!user.inventory.wooden_fishing_rod) user.inventory.wooden_fishing_rod = { amount: 1, durability: null }; if (!user.equippedRodId || !getInventoryAmount(user, user.equippedRodId)) user.equippedRodId = 'wooden_fishing_rod'; if (user.equippedBaitId && !getInventoryAmount(user, user.equippedBaitId)) user.equippedBaitId = null; return user; }
function addInventoryItemDirectly(user, itemId, amount = 1) {
  const delta = Math.max(0, Math.floor(Number(amount) || 0));
  if (delta <= 0) return 0;
  if (!user.inventory[itemId]) {
    const itemDef = ITEMS[itemId];
    user.inventory[itemId] = { amount: delta, durability: itemDef ? itemDef.durability : null };
  } else {
    user.inventory[itemId].amount = (user.inventory[itemId].amount || 0) + delta;
  }
  return user.inventory[itemId].amount;
}
function getUser(userId) { const state = loadState(); const user = ensureUser(state, userId); saveState(state); return JSON.parse(JSON.stringify(user)); }
function updateUser(userId, updater) { const state = loadState(); const user = ensureUser(state, userId); const result = updater(user, state); saveState(state); return result ?? user; }
function getInventoryAmount(user, itemId) { return Math.max(0, Math.floor(Number(user.inventory?.[itemId]?.amount) || 0)); }
function getItemDefinition(itemId) { return ITEMS[itemId] || null; }
function getEquippedRod(user) { return getItemDefinition(user.equippedRodId) || ITEMS.wooden_fishing_rod; }
function getRodPower(rod) { return randomInt(Math.floor(rod.powerMin || 1), Math.floor(rod.powerMax || rod.powerMin || 1)); }
function damageEquippedRod(user, amount) { const rodId = user.equippedRodId || 'wooden_fishing_rod'; const rod = getItemDefinition(rodId); if (!rod || rod.durability === null) return false; const entry = user.inventory[rodId]; if (!entry) return false; entry.durability = Math.max(0, Math.floor(Number(entry.durability ?? rod.durability) - Math.max(0, Math.ceil(amount)))); if (entry.durability > 0) return false; entry.amount = Math.max(0, Math.floor(Number(entry.amount) || 0) - 1); if (entry.amount <= 0) delete user.inventory[rodId]; else entry.durability = rod.durability; user.equippedRodId = 'wooden_fishing_rod'; return true; }
function seasonTime(now = new Date()) { const utc7 = now.getTime() + (7 * 60 * 60 * 1000); const day = Math.floor(utc7 / 86_400_000); const hour = Math.floor((utc7 % 86_400_000) / 3_600_000); const season = SEASONS[Math.floor(day / 2) % SEASONS.length]; const timeKey = ['Morning', 'Noon', 'Afternoon', 'Night'][Math.floor((hour % 12) / 3)]; return { season, time: { key: timeKey, emoji: TIMES[timeKey] } }; }
function rollWeather(seasonKey, timeKey) { const choices = WEATHER_CHANCES[seasonKey]?.[timeKey] || WEATHER_CHANCES.Spring.Morning; const picked = weightedPick(choices.map(([name, emoji, chance]) => ({ value: { name, emoji }, weight: chance }))) || { name: 'Sunny', emoji: '☀' }; const [min, max] = WEATHER_DURATIONS[picked.name] || WEATHER_DURATIONS.Sunny; let durationMinutes = randomInt(min, max); if (durationMinutes < 10) durationMinutes += (10 - durationMinutes) + 10; else if (durationMinutes % 10 !== 0) durationMinutes += 10 - (durationMinutes % 10); return { ...picked, durationMinutes }; }
function getCurrentWeather() { const state = loadState(); const { season, time } = seasonTime(); const now = Date.now(); const slotStart = Math.floor(now / 600_000) * 600_000; const current = state.weather[LOCATION]; if (!current || current.endsAt <= now || current.season !== season.key || current.time !== time.key) { const weather = rollWeather(season.key, time.key); state.weather[LOCATION] = { location: LOCATION, season: season.key, seasonEmoji: season.emoji, time: time.key, timeEmoji: time.emoji, weather: weather.name, weatherEmoji: weather.emoji, startedAt: slotStart, endsAt: slotStart + (weather.durationMinutes * 60_000) }; saveState(state); } return state.weather[LOCATION]; }
function formatForecast(weather) { const effects = WEATHER_TEXT[weather.weather] || WEATHER_TEXT.Sunny; return ['## Fishy Weather Forecast 🐟', `* Season: ${weather.season} ${weather.seasonEmoji}`, `* Time: ${weather.time} ${weather.timeEmoji}`, `* Todays weather: ${weather.weather} ${weather.weatherEmoji}`, '', '-# Effects:', effects.map((effect) => `- ${effect}`).join('\n')].join('\n'); }
async function maybeSendWeatherForecast(client) { const weather = getCurrentWeather(); const key = `${weather.location}:${weather.startedAt}:${weather.weather}`; const state = loadState(); if (state.forecasts.lastForecastKey === key) return; const channel = await client.channels.fetch(FISHING_CHANNEL_ID).catch(() => null); if (!channel?.isTextBased?.()) return; await channel.send(containerPayload(WHITE_ACCENT, [{ type: 10, content: formatForecast(weather) }])).catch(() => null); state.forecasts.lastForecastKey = key; saveState(state); }
function startWeatherTimer(client) { if (weatherTimerStarted) return; weatherTimerStarted = true; maybeSendWeatherForecast(client).catch(() => null); setInterval(() => maybeSendWeatherForecast(client).catch(() => null), 60_000); }
function containerPayload(accent, innerComponents, files = []) { return { flags: COMPONENTS_V2_FLAG, files, components: [{ type: 17, accent_color: accent, components: innerComponents.filter(Boolean) }] }; }
function separator() { return { type: 14, divider: true, spacing: 1 }; }
function actionRow(components) { return { type: 1, components }; }
function mediaGallery(fileName) { return { type: 12, items: [{ media: { url: `attachment://${fileName}` } }] }; }
function findImage(dir, baseName) { if (!fs.existsSync(dir)) return null; const normalized = normalizeId(baseName).replace(/_/g, '').toLowerCase(); for (const file of fs.readdirSync(dir)) { const ext = path.extname(file).toLowerCase(); if (ext !== '.png') continue; if (normalizeId(path.basename(file, ext)).replace(/_/g, '').toLowerCase() === normalized) return path.join(dir, file); } return null; }
function withThumbnail(components, imagePath, fileName, files) { if (!imagePath) return components; files.push(new AttachmentBuilder(imagePath, { name: fileName })); return [mediaGallery(fileName), ...components]; }
function fishActionSelect(userId, placeholder = 'Select an action', values = ['equipment', 'location']) { const labels = { fishing: 'Fishing', equipment: 'Equipments', location: 'Change Location' }; return actionRow([{ type: 3, custom_id: `fish:action:${userId}`, placeholder, min_values: 1, max_values: 1, options: values.map((value) => ({ label: labels[value], value })) }]); }
function fishButton(userId) { return actionRow([{ type: 2, custom_id: `fish:start:${userId}`, label: 'Fish', style: BUTTON_STYLE_SECONDARY }]); }
function renderHome(userId) { const user = getUser(userId); const files = []; const image = findImage(LOCATION_IMAGE_DIR, user.location || LOCATION); const components = withThumbnail([{ type: 10, content: `## You are in ${user.location || LOCATION}\n-# Ready to fish? Press the button below to start fishing` }, separator(), fishButton(userId), fishActionSelect(userId, 'Select an action', ['equipment', 'location'])], image, 'location.png', files); return containerPayload(WHITE_ACCENT, components, files); }
function renderEquipment(userId, username) { const user = getUser(userId); const rod = getEquippedRod(user); const rodOptions = Object.entries(user.inventory).filter(([itemId, entry]) => getItemDefinition(itemId)?.type === 'Gear/Tool' && Number(entry.amount) > 0).map(([itemId]) => { const item = getItemDefinition(itemId); return { label: item.name, value: item.id, emoji: { name: item.emoji }, default: item.id === user.equippedRodId }; }); const baitOptions = Object.entries(user.inventory).filter(([itemId, entry]) => getItemDefinition(itemId)?.type === 'Bait' && Number(entry.amount) > 0).map(([itemId]) => { const item = getItemDefinition(itemId); return { label: item.name, value: item.id, emoji: { name: item.emoji }, default: item.id === user.equippedBaitId }; }); const rodDurability = rod.durability === null ? 'Infinity' : String(user.inventory[user.equippedRodId]?.durability ?? rod.durability); const bait = user.equippedBaitId ? getItemDefinition(user.equippedBaitId) : null; const baitPlaceholder = bait ? bait.name : (baitOptions.length ? 'Choose a bait to use' : "You don't have any bait"); const content = [`## ${username}'s Equipments.`, '-# Fishing rod:', `* Power: ${rod.powerMin} - ${rod.powerMax} - Dur: ${rodDurability}`, '', '-# Bait:', `* Usage: ${bait?.usage || 'No bait equipped'}`].join('\n'); return containerPayload(WHITE_ACCENT, [{ type: 10, content }, actionRow([{ type: 3, custom_id: `fish:rod:${userId}`, placeholder: rod.name, min_values: 1, max_values: 1, disabled: rodOptions.length <= 1, options: rodOptions.length ? rodOptions : [{ label: rod.name, value: rod.id, default: true }] }]), actionRow([{ type: 3, custom_id: `fish:bait:${userId}`, placeholder: baitPlaceholder, min_values: 1, max_values: 1, disabled: baitOptions.length === 0, options: baitOptions.length ? baitOptions : [{ label: "You don't have any bait", value: 'none' }] }]), fishActionSelect(userId, 'Select an action', ['fishing', 'location'])]); }
function renderLocationComingSoon(userId) { return containerPayload(BLACK_ACCENT, [{ type: 10, content: '## coming soon...' }, fishActionSelect(userId, 'Select an action', ['fishing', 'equipment'])]); }
function buildProgressBar(percent) { const safe = Math.max(0, Math.min(100, Math.floor(Number(percent) || 0))); const parts = [BAR.start]; for (let slot = 1; slot <= 9; slot += 1) parts.push(safe >= (slot * 10) ? BAR.middle : BAR.middleEmpty); parts.push(safe >= 100 ? BAR.end : BAR.endEmpty); return parts.join(''); }
function createSession(userId, messageId, fish, weather) { const effect = WEATHER_EFFECTS[weather.weather] || {}; const totalPower = Math.max(1, Math.ceil(fish.powerReq * (effect.powerMultiplier || 1))); const id = `${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`; const buttonCount = RARITY_BUTTONS[fish.rarity] || 2; const buttons = [...REEL_EMOJIS].sort(() => Math.random() - 0.5).slice(0, buttonCount); const correctEmoji = buttons[randomInt(0, buttons.length - 1)]; const session = { id, ownerId: userId, messageId, fishId: fish.id, totalPower, remainingPower: totalPower, startedAt: Date.now(), deadlineAt: Date.now() + 30_000, correctEmoji, buttons, weatherName: weather.weather }; activeGames.set(id, session); return session; }
function renderBite(session) { const fish = FISH_BY_ID.get(session.fishId); const percent = Math.floor(((session.totalPower - session.remainingPower) / session.totalPower) * 100); const files = []; const rows = []; for (let index = 0; index < session.buttons.length; index += 5) rows.push(actionRow(session.buttons.slice(index, index + 5).map((emoji, buttonIndex) => ({ type: 2, custom_id: `fish:reel:${session.id}:${index + buttonIndex}`, emoji: { name: emoji }, style: BUTTON_STYLE_SECONDARY })))); const components = withThumbnail([{ type: 10, content: `## ${fish.emoji} ${fish.name} have bitten your hook!\n${buildProgressBar(percent)} ${percent}%\n\n-# Select the correct button: ${session.correctEmoji}` }, separator(), ...rows], findImage(FISH_IMAGE_DIR, fish.name), `${fish.id}.png`, files); return containerPayload(WHITE_ACCENT, components, files); }
function renderCaught(userId, caughtFish) { const fish = FISH_BY_ID.get(caughtFish.fishId); const files = []; const components = withThumbnail([{ type: 10, content: `## ${fish.emoji} ${fish.name} has been caught!\n-# * Variant: ${caughtFish.variant} ${caughtFish.variantEmoji}\n-# * Weigh: ${caughtFish.weight} kg\n-# * Rarity: ${fish.rarity}` }, separator(), fishButton(userId), fishActionSelect(userId)], findImage(FISH_IMAGE_DIR, fish.name), `${fish.id}.png`, files); return containerPayload(GREEN_ACCENT, components, files); }
function renderEscaped(userId, fish, reason = '') { const files = []; const title = reason === 'broken' || reason === 'lightning' ? `## ${fish.emoji} ${fish.name} has escaped! Your fishing rod broke!` : `## ${fish.emoji} ${fish.name} has escaped!${reason === 'timeout' ? '\nYou taking to long!' : ''}`; const note = reason === 'broken' || reason === 'lightning' ? `-# Should have bought a better fishing rod.${reason === 'lightning' ? '\n-# Woops, looks like your fishing rod got struck by a lightning!' : ''}` : '-# Better luck next time'; const components = withThumbnail([{ type: 10, content: `${title}\n${note}` }, separator(), fishButton(userId), fishActionSelect(userId)], findImage(FISH_IMAGE_DIR, fish.name), `${fish.id}.png`, files); return containerPayload(RED_ACCENT, components, files); }
function pickVariant(weatherName) { if (weatherName === 'Full Moon Night' && Math.random() < 0.1) return VARIANTS[1]; return weightedPick(VARIANTS.map((variant) => ({ value: variant, weight: variant.chance }))) || VARIANTS[0]; }
function chooseFish(weather) { const xlsxWeights = loadXlsxFishWeights(weather.season, weather.time, weather.weather); if (xlsxWeights.length) return weightedPick(xlsxWeights.map((entry) => ({ value: entry.fish, weight: entry.weight }))) || FISH[0]; const rarity = weightedPick(Object.entries(RARITY_WEIGHTS).map(([key, weight]) => ({ value: key, weight }))) || 'common'; const candidates = FISH.filter((fish) => fish.rarity === rarity); return candidates[randomInt(0, candidates.length - 1)] || FISH[0]; }
function loadXlsxFishWeights(seasonKey, timeKey, weatherName) { const column = COLUMN_MAP[seasonKey]?.[timeKey]?.[weatherName]; if (!column || !fs.existsSync(CALM_LAKE_XLSX)) return []; try { const stat = fs.statSync(CALM_LAKE_XLSX); if (!xlsxCache || xlsxCache.mtimeMs !== stat.mtimeMs) xlsxCache = { mtimeMs: stat.mtimeMs, cells: readXlsxCells(CALM_LAKE_XLSX) }; const rows = new Map(); for (const [cell, value] of xlsxCache.cells.entries()) { const match = /^([A-Z]+)(\d+)$/.exec(cell); if (!match) continue; const [, cellColumn, row] = match; const rowNumber = Number(row); if (rowNumber < 4 || rowNumber > 16) continue; if (cellColumn !== 'A' && cellColumn !== column) continue; if (!rows.has(row)) rows.set(row, {}); rows.get(row)[cellColumn] = value; } return [...rows.values()].map((row) => ({ fish: FISH_BY_NAME.get(normalizeName(row.A)), weight: Number(row[column]) })).filter((entry) => entry.fish && Number(entry.weight) > 0); } catch { return []; } }
function readXlsxCells(filePath) { const files = unzipXlsx(fs.readFileSync(filePath)); const sharedStrings = parseSharedStrings(files.get('xl/sharedStrings.xml') || ''); const sheetName = files.has('xl/worksheets/sheet1.xml') ? 'xl/worksheets/sheet1.xml' : [...files.keys()].find((name) => name.startsWith('xl/worksheets/sheet')); return parseSheetCells(files.get(sheetName) || '', sharedStrings); }
function unzipXlsx(buffer) { const files = new Map(); let offset = 0; while (offset < buffer.length - 30) { if (buffer.readUInt32LE(offset) !== 0x04034b50) { offset += 1; continue; } const method = buffer.readUInt16LE(offset + 8); const compressedSize = buffer.readUInt32LE(offset + 18); const nameLength = buffer.readUInt16LE(offset + 26); const extraLength = buffer.readUInt16LE(offset + 28); const name = buffer.slice(offset + 30, offset + 30 + nameLength).toString('utf8'); const dataStart = offset + 30 + nameLength + extraLength; const compressed = buffer.slice(dataStart, dataStart + compressedSize); if (method === 0) files.set(name, compressed.toString('utf8')); else if (method === 8) files.set(name, zlib.inflateRawSync(compressed).toString('utf8')); offset = dataStart + compressedSize; } return files; }
function parseSharedStrings(xml) { const strings = []; for (const match of xml.matchAll(/<si\b[\s\S]*?<\/si>/g)) strings.push([...match[0].matchAll(/<t[^>]*>([\s\S]*?)<\/t>/g)].map((part) => decodeXml(part[1])).join('')); return strings; }
function parseSheetCells(xml, sharedStrings) { const cells = new Map(); for (const match of xml.matchAll(/<c\b([^>]*)>([\s\S]*?)<\/c>/g)) { const attrs = match[1]; const body = match[2]; const ref = /r="([^"]+)"/.exec(attrs)?.[1]; if (!ref) continue; const type = /t="([^"]+)"/.exec(attrs)?.[1]; const raw = /<v>([\s\S]*?)<\/v>/.exec(body)?.[1] ?? ''; const inline = /<t[^>]*>([\s\S]*?)<\/t>/.exec(body)?.[1]; let value = raw; if (type === 's') value = sharedStrings[Number(raw)] ?? ''; else if (type === 'inlineStr') value = decodeXml(inline || ''); cells.set(ref, decodeXml(value)); } return cells; }
function decodeXml(value) { return String(value || '').replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&amp;/g, '&').replace(/&quot;/g, '"').replace(/&apos;/g, "'"); }
async function startFishing(interaction) { const userId = interaction.user.id; const weather = getCurrentWeather(); const effect = WEATHER_EFFECTS[weather.weather] || {}; const lureMs = Math.max(1000, (randomInt(5, 8) + (effect.lureSeconds || 0)) * 1000); await interaction.update(containerPayload(WHITE_ACCENT, [{ type: 10, content: `## You've cast your hook, waiting for fish... ${SPIN_FISH}` }])); setTimeout(async () => { const message = interaction.message; const fish = chooseFish(weather); const session = createSession(userId, message.id, fish, weather); if (weather.weather === 'Thunderstorm' && effect.lightningBreakChance && Math.random() < effect.lightningBreakChance) { const broke = updateUser(userId, (user) => damageEquippedRod(user, Number.MAX_SAFE_INTEGER)); activeGames.delete(session.id); await message.edit(renderEscaped(userId, fish, broke ? 'lightning' : '')).catch(() => null); return; } await message.edit(renderBite(session)).catch(() => activeGames.delete(session.id)); }, lureMs); }
async function handleReel(interaction, sessionId, buttonIndex) { const session = activeGames.get(sessionId); if (!session) { await interaction.reply({ content: 'This fishing attempt already ended.', flags: EPHEMERAL_FLAG }).catch(() => null); return true; } if (interaction.user.id !== session.ownerId) { await interaction.reply({ content: 'This is not your fishing attempt.', flags: EPHEMERAL_FLAG }).catch(() => null); return true; } const fish = FISH_BY_ID.get(session.fishId); if (!fish) return true; if (Date.now() > session.deadlineAt) { activeGames.delete(session.id); await interaction.update(renderEscaped(session.ownerId, fish, 'timeout')); return true; } const selectedEmoji = session.buttons[Number(buttonIndex)]; const weatherEffect = WEATHER_EFFECTS[session.weatherName] || {}; let broke = false; let escaped = false; updateUser(session.ownerId, (user) => { const rod = getEquippedRod(user); const modifiedDur = Math.max(1, Math.ceil(fish.durDamage * (weatherEffect.durMultiplier || 1))); if (selectedEmoji === session.correctEmoji) { session.remainingPower = Math.max(0, session.remainingPower - getRodPower(rod)); broke = damageEquippedRod(user, Math.max(1, Math.ceil(modifiedDur / 4))); } else { session.remainingPower = Math.min(session.totalPower, session.remainingPower + Math.ceil(session.totalPower / 5)); broke = damageEquippedRod(user, modifiedDur); } if (!broke && weatherEffect.escapeChance && Math.random() < weatherEffect.escapeChance) escaped = true; return user; }); if (broke) { activeGames.delete(session.id); await interaction.update(renderEscaped(session.ownerId, fish, 'broken')); return true; } if (escaped) { activeGames.delete(session.id); await interaction.update(renderEscaped(session.ownerId, fish)); return true; } if (session.remainingPower <= 0) { const variant = pickVariant(session.weatherName); const caught = { id: `${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`, fishId: fish.id, weight: Number(randomFloat(fish.minWeight, fish.maxWeight).toFixed(2)), variant: variant.key, variantEmoji: variant.emoji, mutation: null, sellValue: fish.sellValue * variant.multiplier, caughtAt: Date.now() }; updateUser(session.ownerId, (user) => { if (user.fishBarrel.length < user.fishCapacity) user.fishBarrel.push(caught); return user; }); activeGames.delete(session.id); await interaction.update(renderCaught(session.ownerId, caught)); return true; } session.correctEmoji = session.buttons[randomInt(0, session.buttons.length - 1)]; await interaction.update(renderBite(session)); return true; }
function pageItems(items, page, perPage = 5) { const maxPage = Math.max(1, Math.ceil(items.length / perPage)); const safePage = Math.max(1, Math.min(maxPage, Math.floor(Number(page) || 1))); return { page: safePage, maxPage, items: items.slice((safePage - 1) * perPage, safePage * perPage) }; }
function renderInventory(userId, username, page = 1) { const user = getUser(userId); const filter = inventoryFilters.get(userId) || {}; const typeSet = new Set((filter.types || []).map((type) => type.toLowerCase())); const query = String(filter.query || '').toLowerCase(); const records = Object.entries(user.inventory).map(([itemId, entry]) => ({ item: getItemDefinition(itemId), entry })).filter((record) => record.item && Number(record.entry.amount) > 0).filter((record) => typeSet.size === 0 || typeSet.has(record.item.type.toLowerCase())).filter((record) => !query || record.item.name.toLowerCase().includes(query)); const paged = pageItems(records, page); const lines = paged.items.map(({ item, entry }) => { const dur = item.type === 'Gear/Tool' ? `* Durability left: ${item.durability === null ? 'Infinity' : (entry.durability ?? item.durability)}\n` : ''; return `### ×${entry.amount} ${item.name} ${item.emoji} \`${item.type}\`\n${dur}-# Rarity: ${item.rarity}\n-# Value: ${item.value}`; }); return containerPayload(WHITE_ACCENT, [{ type: 10, content: [`## ${username}'s inventory`, lines.join('\n') || '-# No items found.'].join('\n') }, separator(), actionRow([{ type: 2, custom_id: `fish:invfilter:${userId}`, label: 'Filter', style: BUTTON_STYLE_SECONDARY }, { type: 2, custom_id: `fish:invpage:${userId}:${paged.page}:${paged.maxPage}`, label: 'Switch page', style: BUTTON_STYLE_SECONDARY, disabled: paged.maxPage <= 1 }])]); }
function renderFishBarrel(userId, username, page = 1) { const user = getUser(userId); const filter = barrelFilters.get(userId) || {}; const raritySet = new Set((filter.rarities || []).map((rarity) => rarity.toLowerCase())); const query = String(filter.query || '').toLowerCase(); const records = user.fishBarrel.map((entry) => ({ entry, fish: FISH_BY_ID.get(entry.fishId) })).filter((record) => record.fish).filter((record) => raritySet.size === 0 || raritySet.has(record.fish.rarity.toLowerCase())).filter((record) => !query || record.fish.name.toLowerCase().includes(query)); const paged = pageItems(records, page); const lines = paged.items.map(({ entry, fish }) => `### ×1 ${fish.name} ${fish.emoji}\n-# Rarity: ${fish.rarity}\n-# Weigh: ${entry.weight} kg\n-# Variant / Mutation: ${entry.variant} ${entry.variantEmoji} / ${entry.mutation || 'None'}`); return containerPayload(WHITE_ACCENT, [{ type: 10, content: [`## ${username}'s inventory`, `-# Capacity: ${user.fishBarrel.length} / ${user.fishCapacity}`, lines.join('\n') || '-# No fish found.'].join('\n') }, separator(), actionRow([{ type: 2, custom_id: `fish:barrelfilter:${userId}`, label: 'Filter', style: BUTTON_STYLE_SECONDARY }, { type: 2, custom_id: `fish:barrelpage:${userId}:${paged.page}:${paged.maxPage}`, label: 'Switch page', style: BUTTON_STYLE_SECONDARY, disabled: paged.maxPage <= 1 }])]); }
function textInput(customId, label, placeholder, required) { return { type: 1, components: [{ type: 4, custom_id: customId, label, style: 1, required, placeholder, max_length: 100 }] }; }
function pageModal(kind, userId, minPage, maxPage) { return { custom_id: `fish:${kind}pagesubmit:${userId}`, title: 'Switch page', components: [textInput(`fish_${kind}_page`, 'Which page?', `${minPage} - ${maxPage}`, true)] }; }
function filterModal(kind, userId) { if (kind === 'inv') return { custom_id: `fish:invfiltersubmit:${userId}`, title: 'Inventory filter', components: [textInput('fish_inventory_types', 'Sort item type?', 'Gear/Tool, Consumable, Bait, Material', false), textInput('fish_inventory_query', 'Sort by name?', 'Search item name', false)] }; return { custom_id: `fish:barrelfiltersubmit:${userId}`, title: 'Fish Barrel filter', components: [textInput('fish_barrel_rarities', 'Sort fish rarity?', 'Common, uncommon, rare, epic, legendary, mythical, secret', false), textInput('fish_barrel_query', 'Sort by name?', 'Search fish name', false)] }; }
function getField(interaction, customId) { return interaction.fields?.getTextInputValue(customId)?.trim() || ''; }
function parseList(value) { return String(value || '').split(',').map((item) => item.trim()).filter(Boolean); }
function isOwner(interaction, userId) { if (interaction.user.id === userId) return true; interaction.reply({ content: 'Only the command owner can use this control.', flags: EPHEMERAL_FLAG }).catch(() => null); return false; }
async function updateFromInteraction(interaction, payload) { if (typeof interaction.update === 'function') return interaction.update(payload); if (typeof interaction.deferUpdate === 'function') { await interaction.deferUpdate(); return interaction.message?.edit(payload); } return interaction.reply(payload); }
async function handleFishingInteraction(interaction) { const id = interaction.customId || ''; if (!id.startsWith('fish:')) return false; const parts = id.split(':'); const action = parts[1]; if (action === 'action' && interaction.isStringSelectMenu?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; const value = interaction.values?.[0]; if (value === 'equipment') await interaction.update(renderEquipment(userId, interaction.user.username)); else if (value === 'location') await interaction.update(renderLocationComingSoon(userId)); else await interaction.update(renderHome(userId)); return true; } if (action === 'rod' && interaction.isStringSelectMenu?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; updateUser(userId, (user) => { const rodId = interaction.values?.[0]; if (getItemDefinition(rodId)?.type === 'Gear/Tool' && getInventoryAmount(user, rodId)) user.equippedRodId = rodId; return user; }); await interaction.update(renderEquipment(userId, interaction.user.username)); return true; } if (action === 'bait' && interaction.isStringSelectMenu?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; updateUser(userId, (user) => { const baitId = interaction.values?.[0]; if (getItemDefinition(baitId)?.type === 'Bait' && getInventoryAmount(user, baitId)) user.equippedBaitId = baitId; return user; }); await interaction.update(renderEquipment(userId, interaction.user.username)); return true; } if (action === 'start' && interaction.isButton?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; await startFishing(interaction); return true; } if (action === 'reel' && interaction.isButton?.()) return handleReel(interaction, parts[2], parts[3]); if (action === 'invfilter' && interaction.isButton?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; await interaction.showModal(filterModal('inv', userId)); return true; } if (action === 'barrelfilter' && interaction.isButton?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; await interaction.showModal(filterModal('barrel', userId)); return true; } if (action === 'invpage' && interaction.isButton?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; await interaction.showModal(pageModal('inv', userId, 1, parts[4] || 1)); return true; } if (action === 'barrelpage' && interaction.isButton?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; await interaction.showModal(pageModal('barrel', userId, 1, parts[4] || 1)); return true; } if (action === 'invfiltersubmit' && interaction.isModalSubmit?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; inventoryFilters.set(userId, { types: parseList(getField(interaction, 'fish_inventory_types')), query: getField(interaction, 'fish_inventory_query') }); await updateFromInteraction(interaction, renderInventory(userId, interaction.user.username)); return true; } if (action === 'barrelfiltersubmit' && interaction.isModalSubmit?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; barrelFilters.set(userId, { rarities: parseList(getField(interaction, 'fish_barrel_rarities')), query: getField(interaction, 'fish_barrel_query') }); await updateFromInteraction(interaction, renderFishBarrel(userId, interaction.user.username)); return true; } if (action === 'invpagesubmit' && interaction.isModalSubmit?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; await updateFromInteraction(interaction, renderInventory(userId, interaction.user.username, Number(getField(interaction, 'fish_inv_page')))); return true; } if (action === 'barrelpagesubmit' && interaction.isModalSubmit?.()) { const userId = parts[2]; if (!isOwner(interaction, userId)) return true; await updateFromInteraction(interaction, renderFishBarrel(userId, interaction.user.username, Number(getField(interaction, 'fish_barrel_page')))); return true; } return false; }
const fishCommand = { data: new SlashCommandBuilder().setName('fish').setDescription('Go fishing'), suppressCommandLog: true, disableActionTimeout: true, init: startWeatherTimer, async execute(interaction) { await interaction.reply(renderHome(interaction.user.id)); }, async handleInteraction(interaction) { return handleFishingInteraction(interaction); } };
const inventoryCommand = { data: new SlashCommandBuilder().setName('inventory').setDescription('Show your inventory'), suppressCommandLog: true, disableActionTimeout: true, async execute(interaction) { await interaction.reply(renderInventory(interaction.user.id, interaction.user.username)); }, async handleInteraction(interaction) { return handleFishingInteraction(interaction); } };
const fishBarrelCommand = { data: new SlashCommandBuilder().setName('fish-barrel').setDescription('Show your Fish Barrel'), suppressCommandLog: true, disableActionTimeout: true, async execute(interaction) { await interaction.reply(renderFishBarrel(interaction.user.id, interaction.user.username)); }, async handleInteraction(interaction) { return handleFishingInteraction(interaction); } };
const fishBalanceCommand = { data: new SlashCommandBuilder().setName('fish-balance').setDescription('Show your Fish Coin balance'), suppressCommandLog: true, async execute(interaction) { const user = getUser(interaction.user.id); await interaction.reply(containerPayload(WHITE_ACCENT, [{ type: 10, content: [`### ${interaction.user.username}'s Fish Balance`, `* ${user.fishCoins.toLocaleString('en-US')} ${FISH_COIN}`].join('\n') }])); }, async handleInteraction(interaction) { return handleFishingInteraction(interaction); } };
module.exports = { ITEMS, FISH_BY_ID, getUser, updateUser, addInventoryItemDirectly, fishCommand, inventoryCommand, fishBarrelCommand, fishBalanceCommand };
